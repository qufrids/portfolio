# CLAUDE.md

## Project Overview

This is a **portfolio** project repository. It is currently in its initial state with no framework or source code set up yet.

- **Repository name:** portfolio
- **Owner:** Umar Zeb
- **Status:** Initial setup (empty repository)

## Current State

The repository contains only:
- `README.md` — Project title
- `CLAUDE.md` — This file (AI assistant guidance)

No framework, dependencies, or source code have been added yet.

## Development Conventions

When building out this project, follow these conventions:

### General Principles
- Keep code simple and focused; avoid over-engineering
- Prefer editing existing files over creating new ones
- Do not add features, refactoring, or improvements beyond what is requested
- Only add comments where logic is not self-evident
- Validate inputs at system boundaries only (user input, external APIs)

### Git Workflow
- Write clear, descriptive commit messages focused on the "why"
- Do not commit files containing secrets (`.env`, credentials, API keys)
- Use conventional commit style when possible (e.g., `feat:`, `fix:`, `docs:`, `chore:`)

### Code Quality
- Avoid introducing security vulnerabilities (XSS, injection, etc.)
- Do not add unnecessary error handling for impossible scenarios
- Remove unused code completely — no commented-out code or compatibility shims
- Keep dependencies minimal; only add what is necessary

## Project Setup (To Be Defined)

Once the project framework is chosen and initialized, update this file with:
- Technology stack and framework details
- Directory structure explanation
- Available scripts and commands (build, dev, test, lint)
- Environment variable requirements
- Testing strategy and tools
- Deployment configuration
- Styling approach and conventions
- Component/module organization patterns
